import './Polling.scss';

class Polling extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      err: '',
    };
  }

  render() {
    return <div>Polling</div>;
  }
}

export default Polling;
